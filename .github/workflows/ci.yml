name: CI

on:
  push:
  pull_request:

jobs:
  rust-core-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build test image and run rust-core tests
        run: docker compose -f docker/docker-compose.yml run --rm test
