android.useAndroidX=true
kotlin.code.style=official
# Real build error, live-caught: :app:packageDebug failed with OutOfMemoryError (Java heap
# space) while packaging 4 unstripped debug .so libraries (~55-61MB each, ~230MB total --
# see ARCHITECTURE.md/README for why they're currently unstripped: `strip` wasn't available
# in the NDK toolchain path used, a separate TODO). Default Gradle daemon heap isn't enough
# for that much binary data through ApkFlinger's in-memory zip writer.
org.gradle.jvmargs=-Xmx4096m
